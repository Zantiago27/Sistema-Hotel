server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres
    username: postgres.paokukgvnotqbzdpdixy
    password: B4h0tel*#0000
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "TU_SECRET_AQUI_MINIMO_64_CARACTERES"
  expiration-ms: 86400000

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.tuempresa.hotel: DEBUG
